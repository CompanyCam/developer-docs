## List all <%= t(".resource_plural", scope: resource) %>

```shell
<% if t(".nested", scope: resource) %>
curl -X GET -H "Content-type: application/json" -H "Authorization: Bearer <ACCESS_TOKEN>"
https://api.companycam.com/v2/<%= t(".parent_plural", scope: resource) %>/:<%= t(".parent", scope: resource) %>_id/<%= t(".resource_plural", scope: resource) %>/
<% else %>
curl -X GET -H "Content-type: application/json" -H "Authorization: Bearer <ACCESS_TOKEN>"
https://api.companycam.com/v2/<%= t(".resource_plural", scope: resource) %>/
<% end %>
```

```json
<%= json_all %>
```

Returns a list of <%= t(".resource_plural", scope: resource) %> you’ve previously created. The <%= t(".resource_plural", scope: resource) %> are returned in sorted order, with the most recent <%= t(".resource_plural", scope: resource) %> appearing first.

### Filters

| Arguments          | Description
|--------------------|--------------------
| **per_page**          | used for pagination (default is 50)
| **page**           | used for pagination (default is 1)
| **start_date**           | The timestamp of the earliest date a photo was taken
| **end_date**           | The timestamp of the latest date a photo was taken
| **user_ids**           | An array of user ids that captured a photo
| **group_ids**           | An array of group ids that a member of captured a photo
| **project_ids**           | An array of project ids that the photo belongs to
| **tag_ids**           | An array of tag ids that the photo should be tagged with
<% t(".attributes", scope: resource).select{ |k,v| v[:filterable] }.each_pair do |key, value| %>
  **<%= value[:name] %>** | <%= value[:filterable].is_a?(String) ? value[:filterable] : nil %>
<% end %>

### HTTP Request:
<% if t(".nested", scope: resource) %>
`GET  https://api.companycam.com/v2/<%= t(".parent_plural", scope: resource) %>/{<%= t(".parent", scope: resource) %>_id}/<%= t(".resource_plural", scope: resource) %>`
<% else %>
`GET  https://api.companycam.com/v2/<%= t(".resource_plural", scope: resource) %>`
<% end %>
